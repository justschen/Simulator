/**
 * @author Justin Chen
 * @version 1.0
 * @since 2019-03-15
 */
public class Global {

	/**
	 * This is a class that contains static values that are used throughout the code for the simulation.
	 * @params height the given height of the window.
	 * @params width the given width of the window.
	 * @params radius the set radius for the circle in Monte Carlo's simulation.
	 * @params centerX the set center x value for the circle in Monte Carlo's simulation.
	 * @params centerY the set center y value for the circle in Monte Carlo's simulation.
	 * @params buffonLength the set value for a length of a single needle in Buffon's Needle simulation.
	 */
	static int height = 600;
	static int width = 800;
	static int radius = 150;
	static int centerX = width / 2;
	static int centerY = height / 2 - 20;
	static int buffonLength = 50;
	static int midpoint = 0;

	
	
}
